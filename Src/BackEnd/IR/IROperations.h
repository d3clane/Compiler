#ifndef DEF_IR_OP
#define DEF_IR_OP(...)
#endif

// DEF_IR_OP(OP_NAME, ...)

DEF_IR_OP(NOP)

DEF_IR_OP(PUSH)

DEF_IR_OP(POP)

DEF_IR_OP(MOV)

DEF_IR_OP(ADD)

DEF_IR_OP(SUB)

DEF_IR_OP(F_ADD)

DEF_IR_OP(F_SUB)

DEF_IR_OP(F_MUL)

DEF_IR_OP(F_DIV)

DEF_IR_OP(F_AND)

DEF_IR_OP(F_OR)

DEF_IR_OP(F_POW)

DEF_IR_OP(F_SQRT)

DEF_IR_OP(F_SIN)

DEF_IR_OP(F_COS)

DEF_IR_OP(F_TAN)

DEF_IR_OP(F_COT)

DEF_IR_OP(F_PUSH)

DEF_IR_OP(F_POP)

DEF_IR_OP(F_MOV)

DEF_IR_OP(F_CMP)

DEF_IR_OP(JMP)

DEF_IR_OP(JE)

DEF_IR_OP(JNE)

DEF_IR_OP(JL)

DEF_IR_OP(JLE)

DEF_IR_OP(JG)

DEF_IR_OP(JGE)

DEF_IR_OP(CALL)

DEF_IR_OP(RET)

DEF_IR_OP(F_OUT)

DEF_IR_OP(F_IN)

DEF_IR_OP(STR_OUT)

DEF_IR_OP(HLT)
